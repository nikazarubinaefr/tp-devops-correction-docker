---
- name: Run Frontend container
  docker_container:
    name: frontend
    image: nikanika14/tp-devops-frontend:latest
    pull: true
    recreate: true
    state: started
    restart_policy: always
    networks:
      - name: my-network
  vars:
    ansible_python_interpreter: /opt/docker_venv/bin/python
